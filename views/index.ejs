<html>
    <body>
        <h1>
            <%= title %>
        </h1>
        <a href="/new">Add new message</a>
        <div>
            <% messages.forEach((messageItem) => { %>
                <%- include('message', {message: messageItem}) %>
            <% }); %>
        </div>
    </body>
</html>